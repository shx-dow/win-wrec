[package]
name = "wrec-backend"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
wrec-config = { path = "../config" }
wrec-core = { path = "../core" }
wrec-store = { path = "../store" }
tracing.workspace = true
